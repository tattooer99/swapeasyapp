# 🚀 Швидкий старт SwapEasyApp

## ✅ Що вже реалізовано

### Основні функції
- ✅ Додавання кейсів з фото (до 3 штук)
- ✅ Редагування та видалення своїх кейсів
- ✅ Пошук кейсів з фільтрами (категорія, регіон)
- ✅ Система лайків та взаємних інтересів
- ✅ Пропозиції обміну між користувачами
- ✅ Сповіщення про взаємні лайки та пропозиції
- ✅ Чат між користувачами для обговорення обміну
- ✅ Рейтинг користувачів на основі успішних обмінів
- ✅ Історія обмінів
- ✅ Управління інтересами
- ✅ Перегляд кейсів інших користувачів

---

## Крок 1: Встановлення залежностей

```bash
npm install
```

## Крок 2: Налаштування Supabase

1. Зареєструйтесь на [supabase.com](https://supabase.com) (безкоштовно)
2. Створіть новий проект
3. Перейдіть в **SQL Editor**
4. Відкрийте файл `supabase/schema.sql` з цього проекту
5. Скопіюйте весь SQL код та виконайте його
6. Перевірте створені таблиці в **Table Editor**

> **Важливо:** SQL скрипт автоматично додасть поля `rating` та `successful_exchanges` до існуючої таблиці `users`, якщо вони ще не існують.

## Крок 3: Налаштування змінних оточення

1. В налаштуваннях Supabase проекту (Settings → API) скопіюйте:
   - **Project URL** → це буде `VITE_SUPABASE_URL`
   - **anon/public key** → це буде `VITE_SUPABASE_ANON_KEY`

2. Створіть файл `.env` в корені проекту:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-ключ-тут
```

> **Примітка:** Файл `.env` не потрібно комітити в Git (він вже в `.gitignore`)

## Крок 4: Запуск локально

```bash
npm run dev
```

Відкрийте браузер на `http://localhost:3000`

> **Підказка:** Для тестування без Telegram використовуйте браузер - більшість функцій працюватимуть з мок-даними.

## Крок 5: Тестування в Telegram

1. Створіть бота через [@BotFather](https://t.me/botfather):
   - `/newbot` - створіть бота
   - Запам'ятайте токен бота

2. Створіть Mini App:
   - `/newapp` - створіть новий додаток
   - Вкажіть назву та опис
   - Для локального тестування використовуйте [ngrok](https://ngrok.com) або [localtunnel](https://localtunnel.github.io/www/):
     ```bash
     npx localtunnel --port 3000
     ```
   - Скопіюйте отриманий URL та вкажіть його як Web App URL

3. Налаштуйте кнопку меню:
   - `/setmenubutton` в BotFather
   - Виберіть вашого бота
   - Вкажіть текст: "Відкрити SwapEasyApp"
   - Вкажіть URL вашого Mini App

## Крок 6: Деплой на Vercel (рекомендовано)

1. Підключіть GitHub репозиторій до [Vercel](https://vercel.com)
2. Додайте змінні оточення в налаштуваннях проекту:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
3. Деплой відбудеться автоматично
4. Оновіть URL Mini App в BotFather на новий URL від Vercel

### Альтернатива: Railway

Якщо використовуєте Railway, файл `railway.json` вже налаштований. Просто:
1. Підключіть репозиторій до Railway
2. Додайте змінні оточення
3. Деплой відбудеться автоматично

---

## ✅ Готово!

Тепер ваш Telegram Mini App готовий до використання! 🎉

## 📋 Що можна робити далі

### Обов'язково для продакшену:
1. ✅ Налаштувати Supabase (виконати SQL схему) - **ГОТОВО**
2. ✅ Додати змінні оточення - **ГОТОВО**
3. ✅ Задеплоїти на Vercel/Railway - **ГОТОВО**
4. ✅ Підключити до Telegram бота - **ГОТОВО**

### Опціонально (можна додати пізніше):
- **Текстовый поиск** - пошук по назві та опису кейсів (зараз тільки фільтри)
- **Push-уведомления в Telegram** - через бота при взаємних лайках (потребує backend сервер)
- **Избранные пользователи** - підписка на користувачів

## 🔍 Перевірка роботи

Після налаштування перевірте:

1. **Головна сторінка:**
   - Відображається привітання
   - Всі 6 карток доступні (Додати кейс, Переглянути кейси, Інтереси, Пошук, Сповіщення, Історія обмінів)

2. **Додавання кейсу:**
   - Вибір регіону (якщо ще не вибрано)
   - Форма з 5 кроками працює
   - Завантаження фото працює
   - Кнопки "Скасувати" та "Готово" працюють

3. **Пошук:**
   - Фільтри по категорії та регіону працюють
   - Лайки працюють
   - Пропозиції обміну працюють

4. **Сповіщення:**
   - Відображаються взаємні лайки
   - Відображаються пропозиції обміну
   - Кнопки "Прийняти/Відхилити" працюють

5. **Чат:**
   - Відкривається з кнопки "Написати"
   - Повідомлення відправляються та отримуються
   - Автоматичне оновлення кожні 3 секунди

6. **Рейтинг:**
   - Відображається на сторінці кейсів користувача
   - Оновлюється при прийнятті обміну

## 🐛 Вирішення проблем

### Помилка "Supabase не настроен"
- Перевірте, що файл `.env` створено в корені проекту
- Перевірте, що змінні оточення мають правильні значення
- Перезапустіть dev сервер після зміни `.env`

### Помилка "column rating does not exist"
- Виконайте SQL скрипт з `supabase/schema.sql` повністю
- Перевірте, що міграція додавання полів виконалась успішно

### Помилка при завантаженні фото
- Фото зберігаються як base64 строки (для продакшену краще використовувати Supabase Storage)
- Перевірте розмір фото (рекомендовано до 1MB на фото)

### Не працює в Telegram
- Перевірте, що URL Mini App правильний в BotFather
- Перевірте, що бот активний
- Перевірте консоль браузера на помилки

## 📚 Додаткова інформація

- Детальна архітектура: [ARCHITECTURE.md](./ARCHITECTURE.md)
- Статус реалізації: [IMPLEMENTATION_STATUS.md](./IMPLEMENTATION_STATUS.md)
- Повна документація: [README.md](./README.md)

## 🎯 Наступні кроки

Після того, як все працює:

1. **Тестування з реальними користувачами:**
   - Запросіть друзів протестувати додаток
   - Зберіть зворотний зв'язок

2. **Оптимізація:**
   - Налаштуйте Supabase Storage для фото (замість base64)
   - Додайте обмеження на розмір фото
   - Налаштуйте RLS політики для продакшену

3. **Моніторинг:**
   - Налаштуйте логування помилок
   - Додайте аналітику використання

4. **Розширення функціоналу:**
   - Текстовый пошук
   - Push-уведомления (потребує backend)
   - Інші функції з IMPLEMENTATION_STATUS.md

---

**Успіхів з вашим проектом! 🚀**
