# SwapEasyApp - Telegram Mini App

Сучасний Telegram Mini App для обміну речами (бартер) між користувачами без грошей.

## 🚀 Технології

- **React 18** + **TypeScript**
- **Vite** - швидкий білдер
- **Telegram WebApp API** - нативна інтеграція з Telegram (через `window.Telegram.WebApp`)
- **Supabase** - база даних (PostgreSQL)
- **React Router** - навігація

> **Примітка:** Проект використовує нативний Telegram WebApp API замість `@tma.js/sdk` для мінімального розміру бандла та простоти. Для розробки автоматично створюється мок API. Детальніше в [ARCHITECTURE.md](./ARCHITECTURE.md)

## 📋 Функціонал

- ✅ Додавання кейсів з фото (до 3 штук)
- ✅ Пошук кейсів з фільтрами (категорія, регіон)
- ✅ Система лайків та взаємних інтересів
- ✅ Пропозиції обміну
- ✅ Сповіщення про взаємні лайки та пропозиції
- ✅ Управління інтересами
- ✅ Адаптація під світлу/темну тему Telegram

## 🛠 Встановлення та запуск

### 1. Клонування та встановлення залежностей

```bash
npm install
```

### 2. Налаштування Supabase

1. Створіть проект на [supabase.com](https://supabase.com)
2. Перейдіть в **SQL Editor** в панелі управління Supabase
3. Відкрийте файл `supabase/schema.sql` з цього проекту
4. Скопіюйте весь вміст файлу та виконайте його в SQL Editor
5. Перевірте, що всі таблиці створено успішно (перейдіть в **Table Editor**)

> **Примітка:** SQL скрипт автоматично створює всі таблиці, індекси, RLS політики та налаштування доступу.

### 3. Налаштування змінних оточення

Створіть файл `.env` в корені проекту:

```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

Знайдіть ці значення в налаштуваннях проекту Supabase (Settings → API).

### 4. Запуск локально

```bash
npm run dev
```

Відкрийте браузер на `http://localhost:3000`

### 5. Білд для продакшену

```bash
npm run build
```

Файли будуть в папці `dist/`

## 📱 Підключення до Telegram бота

1. Створіть бота через [@BotFather](https://t.me/botfather)
2. Створіть Mini App командою `/newapp`
3. Вкажіть URL вашого задеплоєного додатку (Vercel, Railway, тощо)
4. Налаштуйте кнопку меню бота:

```
/setmenubutton
```

Виберіть бота і вкажіть текст кнопки та URL вашого Mini App.

## 🚢 Деплой

### Vercel

1. Підключіть репозиторій до Vercel
2. Додайте змінні оточення в налаштуваннях проекту
3. Деплой відбудеться автоматично

### Railway

1. Створіть новий проект
2. Підключіть репозиторій
3. Додайте змінні оточення
4. Railway автоматично визначить Vite проект

## 📁 Структура проекту

```
src/
├── components/          # UI компоненти
│   ├── CaseCard.tsx     # Карточка кейсу
│   ├── PhotoUploader.tsx # Завантаження фото
│   └── ChatBubble.tsx    # Повідомлення чату
├── pages/               # Сторінки додатку
│   ├── IndexPage.tsx    # Головна сторінка
│   ├── AddCasePage.tsx  # Додавання кейсу
│   ├── SearchPage.tsx   # Пошук
│   ├── FavoritesPage.tsx # Вподобання та інтереси
│   └── NotificationsPage.tsx # Сповіщення
├── hooks/               # React хуки
│   ├── useTelegram.ts   # Робота з Telegram API
│   └── useSupabase.ts   # Робота з Supabase
├── supabase/            # Supabase клієнт
│   └── client.ts
├── types/               # TypeScript типи
│   ├── index.ts
│   └── telegram.d.ts
├── App.tsx              # Головний компонент
├── main.tsx             # Точка входу
└── index.css            # Глобальні стилі
```

## 🎨 Категорії кейсів

- 🧸 Дитячий світ
- 🚗 Авто
- 🐶 Тварини
- 🏡 Дім і сад
- 📱 Електроніка
- 👗 Одяг
- 🛠 Послуги

## 💰 Цінові категорії

- 0-100 грн
- 100-500 грн
- 500-1000 грн
- 1000-5000 грн
- 5000 грн і більше

## 🔧 Розробка

### Додавання нових функцій

1. Створіть компонент/сторінку в відповідній папці
2. Додайте маршрут в `App.tsx`
3. Оновіть типи в `src/types/index.ts` якщо потрібно
4. Додайте методи в `useSupabase.ts` для роботи з базою

### Стилізація

Використовуйте CSS змінні Telegram для адаптації під тему:
- `var(--tg-theme-bg-color)` - фон
- `var(--tg-theme-text-color)` - текст
- `var(--tg-theme-button-color)` - кнопки
- `var(--tg-theme-hint-color)` - підказки

## 📝 Ліцензія

MIT

## 👨‍💻 Автор

Створено на основі існуючого Telegram бота для обміну речами.

